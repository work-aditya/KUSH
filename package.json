{
  "name": "coachkush-workspace",
  "version": "1.0.0",
  "description": "CoachKush - Elite Online Coaching Platform (React + Vite + Supabase + Razorpay)",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build && node -e \"const fs = require('fs'); fs.cpSync('frontend/dist', 'dist', { recursive: true });\"",
    "preview": "npm --prefix frontend run preview",
    "lint": "npm --prefix frontend run lint",
    "supabase:start": "npx supabase start",
    "supabase:stop": "npx supabase stop",
    "supabase:db:push": "npx supabase db push",
    "supabase:functions:serve": "npx supabase functions serve --env-file supabase/functions/.env"
  }
}
